a17.Behaviors.min = function(container) {
  var features_slider = new a17_slider({
    sliderContainer: $(".features"),
    sliderInner: $(".features .slider"),
    paginator: $(".features .paginator")
  });
};