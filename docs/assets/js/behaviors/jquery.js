a17.Behaviors.jquery = function(container) {
  var features_slider = new a17_slider({
    sliderContainer: $(".features")[0],
    sliderInner: $(".features .slider")[0],
    paginator: $(".features .paginator")[0]
  });
};